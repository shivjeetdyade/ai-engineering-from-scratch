[project]
name = "ai-engineering-from-scratch"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
    "jupyter>=1.0",
]

[project.optional-dependencies]
torch = ["torch>=2.5", "torchvision>=0.20"]
llm = ["anthropic>=0.39", "openai>=1.50"]
